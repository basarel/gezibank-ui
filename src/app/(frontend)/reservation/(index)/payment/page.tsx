import { ReservationLayout } from '@/app/(frontend)/reservation/(index)/_components/page-layout'
import { PaymentPageSection } from '@/app/(frontend)/reservation/(index)/_components/payment-page'

const PaymentPage = () => {
  return (
    <ReservationLayout>
      <PaymentPageSection />
    </ReservationLayout>
  )
}

export default PaymentPage
